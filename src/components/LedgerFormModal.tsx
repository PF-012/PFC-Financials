import React, { useState, useEffect } from 'react';
import { doc, setDoc, getDoc, collection } from '../lib/firebase';
import { db } from '../lib/firebase';
import { useAppContext } from '../context/AppContext';
import { useAuth } from '../context/AuthContext';
import { Ledger, AccountingClassification } from '../types';

interface LedgerFormModalProps { isOpen:boolean; onClose:()=>void; ledgerId?:string|null; onSave?:(ledger:Ledger)=>void; }
const classifications: AccountingClassification[] = ['Revenue','COGS','Operating Expense','Other Income','Finance Cost','Tax Expense','Depreciation','Inventory','Trade Receivable','Trade Payable','Cash & Bank','Fixed Asset','Loan / Debt','Equity / Retained Earnings','Other Asset','Other Liability','Unknown'];
export default function LedgerFormModal({ isOpen,onClose,ledgerId,onSave }:LedgerFormModalProps) {
 const {activeCompany}=useAppContext(); const {user}=useAuth();
 const initialForm:any={name:'',group:'Sundry Debtors',openingBalance:0,cashFlowCategory:undefined,accountingClassification:undefined,address:'',email:'',hsnCode:'',gstin:'',contactNo:'',registrationType:'Regular',gstType:'CGST/SGST',cgstRate:0,sgstRate:0,igstRate:0};
 const [form,setForm]=useState<any>(initialForm); const [loading,setLoading]=useState(false);
 const groups=['Capital Account','Current Assets','Cash-in-Hand','Bank Accounts','Direct Expenses','Direct Incomes','Current Liabilities','Fixed Assets','Indirect Expenses','Indirect Incomes','Purchase Accounts','Sales Accounts','Sundry Creditors','Sundry Debtors','Duties & Taxes'];
 useEffect(()=>{ if(!isOpen)return; if(ledgerId){setLoading(true);getDoc(doc(db,'ledgers',ledgerId)).then(s=>{if(s.exists())setForm({id:s.id,...s.data()});setLoading(false);});}else setForm(initialForm); },[isOpen,ledgerId]);
 const handleSubmit=async(e:React.FormEvent)=>{e.preventDefault();if(!activeCompany||!user)return;if(activeCompany.isBanned&&user.email!=='mndl.yuvi@gmail.com'){alert('Your account has been banned. Please contact support.');return;}try{const ref=ledgerId?doc(db,'ledgers',ledgerId):doc(collection(db,'ledgers'));const newLedger={...form,companyId:activeCompany.id,userId:user.id};if(!ledgerId)delete newLedger.createdAt;await setDoc(ref,newLedger,{merge:true});onSave?.({...newLedger,id:ref.id} as Ledger);onClose();}catch(err){console.error(err);}};
 if(!isOpen)return null;
 return <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4"><div className="bg-white rounded-lg shadow-xl w-full max-w-4xl max-h-[90vh] flex flex-col"><div className="px-6 py-4 border-b border-gray-200 flex justify-between items-center bg-gray-50 shrink-0"><h2 className="text-lg font-medium text-gray-900">{ledgerId?'Edit Ledger':'New Ledger'}</h2><button onClick={onClose} className="text-gray-500 hover:text-gray-700 font-medium">&times;</button></div>{loading?<div className="p-8 text-center text-gray-500">Loading...</div>:<div className="overflow-y-auto flex-1"><form id="ledger-form" onSubmit={handleSubmit} className="p-6 space-y-8"><div><h3 className="text-base font-medium text-gray-900 mb-4">Basic Details</h3><div className="grid grid-cols-1 md:grid-cols-2 gap-6"><div><label className="block text-sm font-medium text-gray-700">Ledger Name *</label><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md sm:text-sm"/></div><div><label className="block text-sm font-medium text-gray-700">Under Group *</label><select required value={form.group} onChange={e=>setForm({...form,group:e.target.value})} className="mt-1 block w-full px-3 py-2 border border-gray-300 bg-white rounded-md sm:text-sm">{groups.map(g=><option key={g}>{g}</option>)}</select></div><div><label className="block text-sm font-medium text-gray-700">Opening Balance</label><input type="number" step="0.01" value={form.openingBalance||''} onChange={e=>setForm({...form,openingBalance:Number(e.target.value)})} className="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md sm:text-sm"/></div><div><label className="block text-sm font-medium text-gray-700">CFO Cash Flow Classification</label><select value={form.cashFlowCategory||''} onChange={e=>setForm({...form,cashFlowCategory:e.target.value||undefined})} className="mt-1 block w-full px-3 py-2 border border-gray-300 bg-white rounded-md sm:text-sm"><option value="">Auto-detect from ledger</option>{['Operating','Other','Investing','Financing'].map(x=><option key={x}>{x}</option>)}</select></div><div className="md:col-span-2"><label className="block text-sm font-medium text-gray-700">Financial Statement Classification</label><select value={form.accountingClassification||''} onChange={e=>setForm({...form,accountingClassification:e.target.value||undefined})} className="mt-1 block w-full px-3 py-2 border border-gray-300 bg-white rounded-md sm:text-sm"><option value="">Auto-detect from group/name</option>{classifications.map(x=><option key={x}>{x}</option>)}</select><p className="mt-1 text-xs text-gray-500">Use an explicit classification for depreciation, finance cost, tax, inventory, debt, fixed assets, equity and other special ledgers. It controls the FRFSA statements.</p></div></div></div></form></div>}<div className="px-6 py-4 border-t border-gray-200 bg-gray-50 flex justify-end gap-3 shrink-0"><button onClick={onClose} className="px-4 py-2 border border-gray-300 rounded-md text-sm text-gray-700 bg-white">Cancel</button><button form="ledger-form" type="submit" className="px-4 py-2 rounded-md text-sm text-white bg-blue-900">Save Ledger</button></div></div></div>;
}
